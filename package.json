{
  "name": "fear-social",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev --config wrangler.local.toml -- vite --port 3000",
    "dev:vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=fear-social --commit-dirty=true --config wrangler.local.toml",
    "db:migrate": "wrangler d1 migrations apply fear-social-db --remote --config wrangler.local.toml",
    "db:migrate:local": "wrangler d1 migrations apply fear-social-db --local --config wrangler.local.toml",
    "icons": "node scripts/generate-icons.mjs"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "sharp": "^0.34.5",
    "vite": "^8.0.14",
    "wrangler": "^4.95.0"
  }
}
